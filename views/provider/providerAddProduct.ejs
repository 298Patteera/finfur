<!-- providerAddProduct.ejs -->
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เพิ่ม/ลบสินค้า</title>
    <link rel="stylesheet" href="/public/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        function toggleCategoryDropdown() {
            document.getElementById('categoryDropdown').classList.toggle('hidden');
        }
        
        function addProduct() {
            let name = document.getElementById('productName').value;
            let category = document.getElementById('categorySelect').value;
            let quantity = document.getElementById('productQuantity').value;
            let unit = document.getElementById('productUnit').value;
            let price = document.getElementById('productPrice').value;
            
            if (!name || !category || !quantity || !unit || !price) {
                alert('กรุณากรอกข้อมูลให้ครบทุกช่อง');
                return;
            }
            
            let productRow = `<tr>
                <td class='border p-2'>${name}</td>
                <td class='border p-2'>${category}</td>
                <td class='border p-2'>${quantity}</td>
                <td class='border p-2'>${unit}</td>
                <td class='border p-2'>${price}</td>
            </tr>`;
            
            document.getElementById('productTable').innerHTML += productRow;
        }
    </script>
</head>
<body class="bg-gray-100">
    <nav class="bg-blue-600 p-4 flex justify-between items-center text-white">
        <div class="flex items-center space-x-4">
            <img src="/public/images/hamburger bar.png" alt="Menu" class="w-8 h-8 cursor-pointer">
            <img src="/public/images/logo.png" alt="FINFUR Logo" class="h-10">
        </div>
        <div class="flex items-center space-x-4">
            <img src="/public/images/search.png" alt="Search" class="w-8 h-8 cursor-pointer">
            <img src="/public/images/account.png" alt="Account" class="w-8 h-8 cursor-pointer">
        </div>
    </nav>
    
    <div class="max-w-6xl mx-auto mt-10 bg-white shadow-lg rounded-lg p-5">
        <h2 class="text-xl font-bold">เพิ่มแก้ไขรายการสินค้า</h2>
        <div class="flex space-x-4 mt-4 border-b pb-2">
            <a href="/provider/providerProductList" class="text-gray-600">สินค้าทั้งหมด</a>
            <a href="/provider/providerAddProduct" class="text-blue-600 border-b-2 border-blue-600 pb-1">เพิ่ม/ลบ สินค้า</a>
            <a href="/provider/providerProductHistory" class="text-gray-600">ประวัติ</a>
        </div>
        
        <div class="mt-4 flex items-center space-x-4">
            <input type="text" placeholder="กรอกรหัสสินค้า หรือ ชื่อสินค้า" class="w-full p-2 border border-gray-300 rounded">
            <div class="relative">
                <button class="bg-blue-600 text-white px-4 py-2 rounded" onclick="toggleCategoryDropdown()">หมวดหมู่</button>
                <div id="categoryDropdown" class="absolute bg-white border mt-2 hidden w-full">
                    <p class="p-2 cursor-pointer hover:bg-gray-200">ทั้งหมด</p>
                    <p class="p-2 cursor-pointer hover:bg-gray-200">ห้องนอน</p>
                    <p class="p-2 cursor-pointer hover:bg-gray-200">ห้องนั่งเล่น</p>
                    <p class="p-2 cursor-pointer hover:bg-gray-200">ห้องครัว</p>
                    <p class="p-2 cursor-pointer hover:bg-gray-200">ห้องรับประทานอาหาร</p>
                    <p class="p-2 cursor-pointer hover:bg-gray-200">ห้องทำงาน</p>
                </div>
            </div>
            <button class="bg-black text-white px-4 py-2 rounded">ค้นหา</button>
        </div>
        
        <table class="mt-6 w-full border">
            <thead>
                <tr class="bg-gray-300">
                    <th class="border p-2">ชื่อสินค้า</th>
                    <th class="border p-2">หมวดหมู่</th>
                    <th class="border p-2">จำนวน</th>
                    <th class="border p-2">หน่วย</th>
                    <th class="border p-2">ราคา</th>
                </tr>
            </thead>
            <tbody id="productTable">
                <tr>
                    <td class="border p-2"><input type="text" id="productName" class="w-full border"></td>
                    <td class="border p-2">
                        <select id="categorySelect" class="w-full border">
                            <option>ห้องนอน</option>
                            <option>ห้องนั่งเล่น</option>
                            <option>ห้องครัว</option>
                            <option>ห้องรับประทานอาหาร</option>
                            <option>ห้องทำงาน</option>
                        </select>
                    </td>
                    <td class="border p-2"><input type="number" id="productQuantity" class="w-full border"></td>
                    <td class="border p-2"><input type="text" id="productUnit" class="w-full border"></td>
                    <td class="border p-2"><input type="number" id="productPrice" class="w-full border"></td>
                </tr>
            </tbody>
        </table>
        
        <div class="flex mt-4 space-x-4">
            <button class="bg-red-600 text-white px-4 py-2 rounded">ลบสินค้า</button>
            <button class="bg-green-600 text-white px-4 py-2 rounded" onclick="addProduct()">เพิ่มสินค้า</button>
        </div>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>สมัครสมาชิก | FINFUR</title>
    <link rel="icon" type="image/png" href="/images/logo_main.png">

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = { darkMode: 'class' }; // ใช้ dark mode ผ่าน class
    </script>

    <!-- Custom Style -->
    <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-white flex justify-center items-center h-screen dark:bg-gray-900 dark:text-white">

    <!-- Sign-in Form -->
    <div class="flex justify-center items-center h-screen pt-20">
        <div id="navbarContainer" class="w-full fixed top-0 left-0 z-50 bg-white shadow-md">
            <div id="defaultNavbar">
                <%- include('partials/narbar') %>
            </div>
            <div id="searchNavbar" style="display: none;">
                <%- include('partials/narbar_search') %>
            </div>
        </div>
        <div class="w-full max-w-md mx-auto mt-24 pt-10">

            <h3 class="text-3xl font-bold text-black mb-4 dark:text-white">สมัครสมาชิก</h3>

            <form action="/signin-customer" method="POST" class="border shadow-md rounded-lg p-6 space-y-4">
                <div>
                    <label class="block font-medium text-black dark:text-white">username*</label>
                    <input type="text" name="username" class="dark:text-black w-full border border-gray-300 rounded-lg p-2 mt-1 focus:ring focus:ring-blue-300" required>
                </div>
            
                <div>
                    <label class="block font-medium text-black dark:text-white">ชื่อ-นามสกุล*</label>
                    <input type="text" name="name" class="dark:text-black w-full border border-gray-300 rounded-lg p-2 mt-1 focus:ring focus:ring-blue-300" required>
                </div>
            
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block font-medium text-black dark:text-white">อีเมล*</label>
                        <input type="email" name="email" class="dark:text-black w-full border border-gray-300 rounded-lg p-2 mt-1 focus:ring focus:ring-blue-300" required>
                    </div>
                    <div>
                        <label class="block font-medium text-black dark:text-white">เบอร์โทรศัพท์*</label>
                        <input type="text" name="phone" class="dark:text-black w-full border border-gray-300 rounded-lg p-2 mt-1 focus:ring focus:ring-blue-300" required>
                    </div>
                </div>
            
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block font-medium text-black dark:text-white">เพศ*</label>
                        <select name="gender" class="dark:text-black w-full border border-gray-300 rounded-lg p-2 mt-1 focus:ring focus:ring-blue-300" required>
                            <option value="male">ชาย</option>
                            <option value="female">หญิง</option>
                            <option value="other">อื่นๆ</option>
                        </select>
                    </div>
                    <div>
                        <label class="block font-medium text-black dark:text-white">วันเกิด*</label>
                        <input type="date" name="dob" class="dark:text-black w-full border border-gray-300 rounded-lg p-2 mt-1 focus:ring focus:ring-blue-300" required>
                    </div>
                </div>
            
                <div>
                    <label class="block font-medium text-black dark:text-white">รหัสผ่าน*</label>
                    <input type="password" name="pssword" class="dark:text-black w-full border border-gray-300 rounded-lg p-2 mt-1 focus:ring focus:ring-blue-300" required>
                </div>
            
                <div>
                    <label class="block font-medium text-black dark:text-white">ยืนยันรหัสผ่าน*</label>
                    <input type="password" name="confirmPssword" class="dark:text-black w-full border border-gray-300 rounded-lg p-2 mt-1 focus:ring focus:ring-blue-300" required>
                </div>
            
                <div class="flex items-center">
                    <input type="checkbox" id="agreeTerms" name="agree_terms" class="form-checkbox border border-gray-400 text-blue-600 rounded-md">
                    <span class="ml-2 text-sm text-black dark:text-white">
                        ยอมรับข้อกำหนดเงื่อนไขและ <a href="#" class="text-blue-600 underline" id="termsLink">นโยบายความเป็นส่วนตัว</a>
                    </span>
                </div>
            
                <button type="submit" id="submitBtn" class="w-full bg-blue-600 text-white text-lg font-semibold py-3 rounded-lg hover:bg-blue-700">
                    สมัครสมาชิก
                </button>
            </form>            
        </div>
    </div>

<!-- Popup ข้อกำหนดเงื่อนไข -->
<div id="termsModal" class="fixed inset-0 bg-gray-800 bg-opacity-50 flex justify-center items-center hidden">
    <div class="bg-white w-96 p-6 rounded-lg shadow-lg">
        <h2 class="text-xl font-semibold mb-4">ข้อกำหนดและเงื่อนไข</h2>
        <p class="text-sm text-black dark:text-white mb-4">
            ยินดีต้อนรับสู่เว็บไซต์ของเรา! ก่อนที่ท่านจะสมัครสมาชิก กรุณาอ่านข้อกำหนดและเงื่อนไขการใช้บริการ
            โดยข้อกำหนดเหล่านี้จะควบคุมการใช้งานและการจัดการข้อมูลส่วนบุคคลของท่าน
        </p>
        <p class="text-sm text-black dark:text-white mb-4">
            1. ข้อมูลที่ท่านกรอกต้องเป็นความจริงและถูกต้อง
        </p>
        <p class="text-sm text-black dark:text-white mb-4">
            2. เว็บไซต์ของเราจะไม่เปิดเผยข้อมูลส่วนบุคคลของท่านให้แก่บุคคลที่สาม ยกเว้นในกรณีที่กฎหมายกำหนด
        </p>
        <p class="text-sm text-black dark:text-white mb-4">
            การใช้บริการของเราแสดงว่าท่านยอมรับข้อกำหนดเหล่านี้
        </p>

        <div class="flex justify-between mt-4">
            <button id="closeModal" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">ปิด</button>
        </div>
    </div>
</div>


    <script>
        
    // เรียก Modal เมื่อคลิกลิงก์ "นโยบายความเป็นส่วนตัว"
    document.getElementById('termsLink').addEventListener('click', function(event) {
        event.preventDefault();
        document.getElementById('termsModal').classList.remove('hidden');
    });

    // ปิด Modal เมื่อคลิก "ตกลง" หรือ "ยกเลิก"
    document.getElementById('closeModal').addEventListener('click', function() {
        document.getElementById('termsModal').classList.add('hidden');
    });

        // รับค่าของ checkbox และปุ่ม submit
        const agreeCheckbox = document.getElementById('agreeTerms');
    const submitButton = document.getElementById('submitBtn');

    // ฟังก์ชันตรวจสอบเมื่อผู้ใช้กดปุ่ม "สมัครสมาชิก"
    submitButton.addEventListener('click', function(event) {
        if (!agreeCheckbox.checked) {
            event.preventDefault();  // ป้องกันการส่งฟอร์ม
            alert("ท่านต้องกดยอมรับนโยบายก่อน");  // แสดงข้อความเตือน
        }
    });

        function toggleSearch(event) {
            event.preventDefault();
            let searchMode = sessionStorage.getItem("searchMode") === "true";
            sessionStorage.setItem("searchMode", !searchMode);

            updateNavbar(); // เรียกฟังก์ชันเปลี่ยน navbar
        }

        function updateNavbar() {
            let searchMode = sessionStorage.getItem("searchMode") === "true";
            document.getElementById("defaultNavbar").style.display = searchMode ? "none" : "block";
            document.getElementById("searchNavbar").style.display = searchMode ? "block" : "none";
        }

        // ตรวจสอบเมื่อโหลดหน้า
        document.addEventListener("DOMContentLoaded", updateNavbar);
    </script>
</body>
</html>
